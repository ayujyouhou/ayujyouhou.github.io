---
const { href, src, label } = Astro.props;
---
<a href={href} target="_blank" rel="noopener" class="group inline-flex items-center gap-3 rounded-xl border px-4 py-2 hover:shadow transition">
  <img src={src} alt={label} class="size-6 rounded" />
  <span class="text-sm">{label}</span>
</a>
