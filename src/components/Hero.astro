---
const { title = "Ayu", subtitle = "Engineer / Creator", avatar = "/images/profile.jpg" } = Astro.props;
---
<section class="relative h-[56vh] min-h-[320px] flex flex-col items-center justify-center text-center">
<img src={avatar} alt="avatar"
     class="w-32 h-32 md:w-40 md:h-40 rounded-full 
            object-cover object-[50%_35%]   <!-- ← 中心を少し上に -->
            scale-130                        <!-- ← 少しズームイン -->
            shadow-xl ring-4 ring-white/60" />


  <h1 class="mt-6 text-3xl md:text-5xl font-extrabold text-gray-900 drop-shadow-sm">{title}</h1>
  <p class="mt-2 text-base md:text-lg text-gray-700">{subtitle}</p>
</section>

