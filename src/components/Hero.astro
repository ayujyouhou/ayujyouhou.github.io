---
const { title = "Ayu", subtitle = "Engineer / Creator", avatar = "/images/profile.jpg" } = Astro.props;
---
<section class="relative h-[40vh] min-h-[320px] flex flex-col items-center justify-center text-center">
<img src={avatar} alt="avatar"
     class="w-40 h-40 md:w-56 md:h-56 rounded-full 
            object-cover object-[50%_35%]   <!-- ← 中心を少し上に -->
            scale-130                        <!-- ← 少しズームイン -->
            shadow-xl ring-4 ring-white/60" />


  <h1 class="mt-6 text-4xl md:text-6xl font-extrabold text-gray-900 drop-shadow-sm">{title}</h1>
  <p class="mt-2 text-base md:text-lg text-gray-700">{subtitle}</p>
</section>

